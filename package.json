{
	"scripts": {
		"dev": "concurrently \"yarn server\" \"yarn client\"",
		"server": "cd backend && yarn dev",
		"client": "cd frontend && yarn start"
	},
	"dependencies": {},
	"devDependencies": {
		"concurrently": "^9.1.0"
	}
}
